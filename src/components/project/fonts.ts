import { DM_Sans, Montserrat } from "next/font/google";

// Fonts used by the Framer case-study pages (wellim, illustrations).
export const dmSans = DM_Sans({
  subsets: ["latin"],
  weight: ["300", "400", "500"],
});

export const montserrat = Montserrat({
  subsets: ["latin"],
  weight: ["400", "500"],
});
